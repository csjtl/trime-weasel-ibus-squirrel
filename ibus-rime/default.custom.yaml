patch:
  "menu/page_size": 9
  schema_list:
    - { schema: easy_en_super }
    - { schema: easy_en }
    - { schema: pinyin_simp }
    - { schema: wubi06_cjk }

  "switcher/hotkeys": # 这个列表里每项定义一个快捷键，使哪个都中
    #- "Control+s"     # 添加 Ctrl+s
    - "Control+grave" # 你看写法并不是 Ctrl+` 而是与 IBus 一致的表示法
    - "Control+F3"
